<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AnnouncementMapper">

    <insert id="insert" parameterType="com.example.entity.Announcement" useGeneratedKeys="true">
        insert into `announcement`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="title != null">title,</if>
            <if test="content != null">content,</if>
            <if test="time != null">time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="title != null">#{title},</if>
            <if test="content != null">#{content},</if>
            <if test="time != null">#{time},</if>
        </trim>
    </insert>

    <delete id="deleteById">
        delete from `announcement`
        where id = #{id}
    </delete>

    <update id="updateById" parameterType="com.example.entity.Announcement">
        update `announcement`
        <set>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="time != null">
                time = #{time},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="selectAll" resultType="com.example.entity.Announcement">
        select * from `announcement`
        <where>
            <if test="title != null"> and title like concat('%', #{title}, '%')</if>
        </where>
    </select>

</mapper>